{
  "name": "maginai",
  "version": "0.1.0",
  "author": "Spoonail",
  "repository": {
    "type": "git",
    "url": "https://github.com/Spoonail-Iroiro/maginai"
  },
  "files": [
    "lib/"
  ],
  "type": "module",
  "main": "",
  "types": "./lib/loader.d.ts",
  "scripts": {
    "dev": "npx genversion --es6 ./js/mod/modules/version.js && vite",
    "build": "npx genversion --es6 ./js/mod/modules/version.js && vite build",
    "preview": "vite preview",
    "release": "npm run build && npx tsc && node ./tools/release.js && node ./tools/releaseSampleMod.js"
  },
  "devDependencies": {
    "genversion": "^3.1.1",
    "typescript": "^5.2.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "loglevel": "^1.8.1",
    "loglevel-plugin-prefix": "^0.8.4"
  }
}
